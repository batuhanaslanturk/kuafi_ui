import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0D1B2A); // Ana Arka Plan
  static const Color card = Color(0xFF1B263B); // İkincil Arka Plan / Kartlar
  static const Color accent = Color(0xFFD4AF37); // Vurgu Rengi
  static const Color textPrimary = Color(0xFFE0E1DD); // Yazı Rengi (Açık)
  static const Color textSecondary = Color(0xFF778DA9); // Yazı Rengi (Pasif/İkincil)
  static const Color buttonPrimary = Color(0xFF581845); // Buton Arka Planı (Primary)
  static const Color buttonAlt = Color(0xFF121212); // Alternatif Buton Arka Planı
  static const Color success = Color(0xFF9EB23B); // Başarı (Success)
  static const Color warning = Color(0xFFFFB400); // Uyarı (Warning)
}
